# Fun and polish cycle

**Goal:** strengthen visible discovery and narrative payoff in the existing two cases, using `Docs/QualityReview.md`. This is a bounded extension of the existing workbench, not a new mechanic or case. The user has authorized routine design decisions and debug APK validation.

**Baseline:** `b5444412a8ddf00aa59d0f27a19a42d17bcfdf55`; product code previously built at `6437d57`. Preserve the positive returning-player puzzle feedback, the 28 actions and all stable save/scene IDs. Existing personal emulator and old builds remain untouched. Unity 6000.3.21f1, portrait, API29/36, ARM64 IL2CPP, offline, EN/KO, debug signing/sample ad IDs; no release/AAB/merge.

1. **Visible watch discovery.** Current Ice02 completion/early Ice03 use the leafless default sprite; Ice03 changes artwork only after `free-leaf`, although `clear-hinge` already opens the lid. Add a three-state watch atlas and select closed-with-leaf at Ice02 completion/early Ice03, open-with-leaf immediately after `clear-hinge`, and open-empty after `free-leaf`. Root owns `WorkbenchArtwork.cs`, relevant builder configuration and `WorkbenchPlayModeTests.cs`. First test actual UI sprite continuity across scene transition, immediate change after each action, and restart returning to the initial state. Pixel-content quality is checked separately from state-routing tests.
2. **Narrative payoff and reading.** Content agent changes only four Rain03–05 EN/KO prose fields: the letter asks that the umbrella keep helping others, directs the reply to its pocket, and changes the senior's motive from waiting to return it to passing care onward. Root fixes Korean mid-word wrapping based on a failing rendered-text test at 1080×1920 and 1080×2400 equivalent layout. No new literal-copy test; use existing bilingual content, endings, and layout integration checks. Reference media and text provenance are recorded before authoring.
3. **Integrate and play.** Root is the only Unity/device owner. Run focused RED/green tests and the repository full Editor suite once at final source, BuildAll via debug builder, and compile-condition checks when affected. Preserve build companions; use a unique FunPolish output. Verify APK source/hash/settings, install only on dedicated QA AVD with before/after save/hash evidence, and exercise affected watch actions, Rain endings, KO/EN, offline and restart/replay. Independent reviewer checks before/after screenshots without claiming human fun validation. Record selected/rejected design effects, open unrelated findings, handoff guide and existing PR update.

**Acceptance:** watch clue is recognizable and remains the same object between stages; lid and leaf changes occur at the described action; Korean words remain together without overflow or smaller-than-supported text; letter-to-umbrella motive and physical destination agree; existing progress remains compatible. Verification records technical behavior, agent visual/editorial assessment and human feedback separately. Source and assets freeze while final tests/build run.
